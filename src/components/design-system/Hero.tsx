export default function Hero() {
    return (
        <section className="hero">
            <div className="hero-tag">Design System Teardown</div>
            <h1 className="hero-title">
                Styling, Format <br />
                Molecular Structure
            </h1>
            <p className="hero-sub">
                A full breakdown of Calvin Klein's visual design language, atomic component system, typography hierarchy, spacing tokens, and page anatomy — pulled from their product detail page experience. Built to steal.
            </p>
        </section>
    );
}
